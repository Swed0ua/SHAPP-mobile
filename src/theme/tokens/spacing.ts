import type { ThemeSpacing } from "../types";

/** Layout rhythm: use these instead of raw numbers in components. */
export const spacing: ThemeSpacing = {
  none: 0,
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
};
